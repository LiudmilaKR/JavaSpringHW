spring:
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

server:
  error:
    whitelabel:
      enabled: false

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: metrics, prometheus
